

import random


def randomNumberGuess():
    random_number = random.randint(1, 10)
    # set a count in memory to keep track of attempts

    print("Welcome to the Number Guessing Game!")
    print("I'm thinking of a number between 1 and 10.")

    while True:
        try:
          # Get the users guess

          # Add 1 count to the total attempts

            if
            print("Too low! Try again.")
            elif
            print("Too high! Try again.")
            else:
              # Print the response using template literals

                break
        except ValueError:
            print("Please enter a valid number.")


# Call the function to start the guessing
randomNumberGuess()
