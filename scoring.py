
# The program should handle input errors gracefully and ensure the score is within a valid range (0 to 100). Make the scores increment by 10's 90 - 100 is an A, under 60 is an F.

score = float(input("Enter the score (0-100): "))

# use the if, elif, else statement

print("The grade is: {grade}")
